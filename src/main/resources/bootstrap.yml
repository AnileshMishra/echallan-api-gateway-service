spring:
  cloud:
    config:
      uri:
      - http://localhost:8191     
management:
   endpoint:
      gateway:
         enabled: true
   endpoints:
      web:
         exposure:
            include: gateway